import { AutoErrorsDirective } from './auto-errors.directive';

describe('AutoErrorsDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoErrorsDirective();
    expect(directive).toBeTruthy();
  });
});
