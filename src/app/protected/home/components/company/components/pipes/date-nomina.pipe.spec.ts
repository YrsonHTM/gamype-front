import { DateNominaPipe } from './date-nomina.pipe';

describe('DateNominaPipe', () => {
  it('create an instance', () => {
    const pipe = new DateNominaPipe();
    expect(pipe).toBeTruthy();
  });
});
