import { BeatyDatePipe } from './beaty-date.pipe';

describe('BeatyDatePipe', () => {
  it('create an instance', () => {
    const pipe = new BeatyDatePipe();
    expect(pipe).toBeTruthy();
  });
});
